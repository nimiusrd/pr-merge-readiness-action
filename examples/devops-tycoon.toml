# action_ref は利用する release の実 SHA に置き換えてください。
version = 1
action_ref = "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"

[ci]
workflows = ["CI"]

[review]
minimum_approvals = 0
require_resolved_threads = true
stale_change_review_days = 30

[publication]
checks = true
labels = "manual"

[[ci.required_checks]]
kind = "check_run"
name = "Lint & Unit (Vitest)"
app_id = 15368

[[ci.required_checks]]
kind = "check_run"
name = "E2E (Playwright)"
app_id = 15368
