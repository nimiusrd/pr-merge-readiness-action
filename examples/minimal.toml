# 設定 version 2 対応の実装 SHA。workflow の uses も同じ SHA に指定します。
version = 2
action_ref = "27ca8908b993e93eb319c70e7231fa7fd1999b05"

[review]
minimum_approvals = 0
require_resolved_threads = true
stale_change_review_days = 30

[publication]
checks = true
labels = "manual"
