# 次版の公開後、workflow の uses と同じ40桁の実装 SHA を指定します。
version = 2
action_ref = "REPLACE_WITH_RELEASE_COMMIT_SHA"

[review]
minimum_approvals = 1
require_resolved_threads = true
stale_change_review_days = 14

[publication]
checks = true
labels = "manual"
