# 架空の複数 workflow 構成例です。特定のリポジトリの設定ではありません。
# workflow・Check 名と action_ref を利用先の実際の値に置き換えてください。
version = 1
action_ref = "21fa2df95fc615847ba7e1a24e9c77bd5d1323fb"

[ci]
workflows = ["Build", "Quality", "Security"]

[review]
minimum_approvals = 0
require_resolved_threads = true
stale_change_review_days = 30

[publication]
checks = true
labels = "manual"

[[ci.required_checks]]
kind = "check_run"
name = "Linux build"
app_id = 15368

[[ci.required_checks]]
kind = "check_run"
name = "Windows build"
app_id = 15368

[[ci.required_checks]]
kind = "check_run"
name = "Unit tests"
app_id = 15368

[[ci.required_checks]]
kind = "check_run"
name = "End-to-end tests"
app_id = 15368

[[ci.required_checks]]
kind = "check_run"
name = "Lint"
app_id = 15368

[[ci.required_checks]]
kind = "check_run"
name = "Format"
app_id = 15368

[[ci.required_checks]]
kind = "check_run"
name = "Dependency audit"
app_id = 15368
